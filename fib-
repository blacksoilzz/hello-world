def fib(n):
    fib_curr,fib_next = 1, 1
    if n < 2:
        return 1
    else:
        ret = [1]
        for i in range(n):
            fib_next, fib_curr = fib_curr+fib_next, fib_next
            ret.append(fib_curr)

    return ret
